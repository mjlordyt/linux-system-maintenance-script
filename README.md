# linux-system-maintenance-script
The script performs a complete system maintenance cycle using standard Linux package management commands. It refreshes package repositories, fixes broken dependencies, upgrades installed packages, performs a full upgrade when required, removes unused packages, cleans cached files, and finally displays system disk usage and uptime information.
