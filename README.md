# 🚀 Linux System Maintenance Script (One-Click Automation)

A simple yet powerful **Bash automation script** for **APT-based Linux distributions** such as Ubuntu, Debian, Kali Linux, and Parrot OS. This script allows users to perform all essential Linux system maintenance operations in a single run, ensuring the system remains updated, clean, and stable.

---

## ✨ Features

✔ Updates package lists  
✔ Fixes broken packages automatically  
✔ Upgrades installed packages  
✔ Handles dependency changes safely  
✔ Removes unused dependencies  
✔ Cleans package cache  
✔ Displays disk usage and system uptime  
✔ Fully automated one-click execution  

---

## 🛠 Supported Distributions

This script works on all **APT-based Linux systems**, including:
Ubuntu, Debian, Kali Linux, Parrot OS, and similar distributions.

---

## ⚡ How to Run the Script

Clone the repository, navigate to the directory, grant execute permissions, and run the script using the following commands:

```bash
cd linux-system-maintenance-script && \
chmod +x system_maintenance.sh && \
./system_maintenance.sh
